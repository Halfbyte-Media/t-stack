# T-Stack local worktrees (created at runtime, not committed)
.tstack/worktrees/

# T-Stack migration state (local per developer, created by /setup)
.tstack/.migrated

# CLI staging directory (populated at build/publish time)
packages/cli/files/

# Private markdown files
**/*.md